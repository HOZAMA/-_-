Для пересборки индексов используються команды

```console
indexer --rotate --verbose --all // в случае пересборки всех индексов
```

```console
indexer --rotate --verbose index_1 index_2 index_3 // в случае определнных индексов 
```